fis.config.merge({
    namespace: 'i18n',
    modules: {
        parser: {
            po: 'po'
        }
    },
    roadmap: {
        ext: {
            po: 'json'
        }
    }
});

fis.config.get('roadmap.path').unshift({
    reg: /\/lang\/([^\/]+)\.po/i,
    release: '/config/lang/${namespace}.$1.po'
});
